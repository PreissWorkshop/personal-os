# Secrets backstop — this repo must never carry credentials.
.env
.env.*
*.env
*private_key*.xml
*private-key*.xml
*_signing_key*.xml
*token*.txt
secrets/
*.pem
*.key

# Private finance data - the money skill reads it from OUTSIDE every repo
# (this repo is public). Only the zero-filled template may be committed.
finance/
*finance-snapshot*.json
!**/finance-snapshot.template.json
